% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod-column-picker.R
\name{column_picker}
\alias{column_picker}
\alias{column_picker_ui}
\alias{column_picker_server}
\title{Column Picker Module}
\usage{
column_picker_ui(
  id,
  label,
  choices = character(),
  selected = character(),
  width = "100\%",
  plugins = c("drag_drop", "remove_button"),
  placeholder = "Select columns...",
  bulk_actions = TRUE
)

column_picker_server(id, get_choices, initial_value = character())
}
\arguments{
\item{id}{Module ID (must match the \code{id} passed to
\code{column_picker_ui}).}

\item{label}{Label text displayed above the selectize input.}

\item{choices}{Initial choices (character vector).}

\item{selected}{Initially selected values (character vector).}

\item{width}{CSS width for the selectize input (default \code{"100\%"}).}

\item{plugins}{Selectize plugins to enable (default: drag_drop, remove_button).}

\item{placeholder}{Placeholder text when no columns are selected.}

\item{bulk_actions}{Logical. Show "All | None" action links (default TRUE).}

\item{get_choices}{Reactive returning available column names
(e.g., \code{reactive(colnames(data()))}).}

\item{initial_value}{Character vector or \code{reactiveVal}. When a
\code{reactiveVal} is supplied the module two-way-binds to it
(external changes update the UI).}
}
\value{
\code{column_picker_ui} returns a \code{shiny.tag}.

A \code{reactiveVal} holding the current column selection.
}
\description{
A reusable Shiny module for selecting multiple columns with bulk actions
(Select All / Select None) and drag-and-drop reordering.
}
\details{
This module is intended to eventually move to a \code{blockr.ui} package.
For now it lives in blockr.dplyr so that blocks here (and blockr.bi
via import) can use it.
}
