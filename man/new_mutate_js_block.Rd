% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_js.R
\name{new_mutate_js_block}
\alias{new_mutate_js_block}
\title{JS-driven mutate expression block (prototype)}
\usage{
new_mutate_js_block(exprs = list(new_col = "1"), ...)
}
\arguments{
\item{exprs}{Named list of expressions (name = column name, value = R expression)}

\item{...}{Additional arguments forwarded to \code{\link[blockr.core:new_block]{blockr.core::new_block()}}}
}
\value{
A block object for mutate operations
}
\description{
Like \code{\link[=new_mutate_expr_block]{new_mutate_expr_block()}} but all row management happens in JavaScript.
R is only contacted on Submit — to parse and evaluate the expressions.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(new_mutate_js_block(), data = list(data = mtcars))
}
}
