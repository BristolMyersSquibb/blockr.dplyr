% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-builders.R
\name{build_column_values_lazy}
\alias{build_column_values_lazy}
\title{Lazy (remote-table) variant of \code{\link[=build_column_values]{build_column_values()}}}
\usage{
build_column_values_lazy(df, col, limit = NULL, query = "")
}
\arguments{
\item{df}{A data frame}

\item{col}{Column name (character)}

\item{limit}{Maximum number of unique values to return (\code{NULL} = all)}

\item{query}{Substring to filter values by (case-insensitive)}
}
\value{
A column info object (list), matching \code{\link[=build_column_values]{build_column_values()}}.
}
\description{
Same return contract as \code{\link[=build_column_values]{build_column_values()}}, but never pulls a whole
column into R. Numeric columns report only their \code{min}/\code{max} (one aggregate
query) and offer no value list — they filter by range. Character columns use
a \emph{bounded distinct}: at most \code{limit + 1} distinct values are pulled
(\verb{SELECT DISTINCT col ... LIMIT k+1}), so cost is capped by \code{limit}, not by
the column's cardinality. Pulling \code{limit + 1} doubles as the "too many to
enumerate" probe: if the sentinel row comes back, \code{truncated = TRUE} and the
client switches to server-side search (each keystroke pushes a \code{LIKE} down
via \code{query}). All of this runs only when the user opens a column's picker.
}
\keyword{internal}
