% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutate_unified.R
\name{new_mutate_unified_block}
\alias{new_mutate_unified_block}
\title{Unified JS-driven mutate block}
\usage{
new_mutate_unified_block(exprs = list(new_col = "1"), ...)
}
\arguments{
\item{exprs}{Named list of expressions (e.g., \code{list(mpg2 = "mpg * 2")})}

\item{...}{Additional arguments forwarded to \code{\link[blockr.core:new_block]{blockr.core::new_block()}}}
}
\value{
A block object for mutate operations
}
\description{
Expression-only mutate block with a unified visual design matching
the filter unified block. Each row has a name input and an ACE editor
for the R expression. Changes require explicit Enter confirmation.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(new_mutate_unified_block(), data = list(data = mtcars))
}
}
