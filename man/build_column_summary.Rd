% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-builders.R
\name{build_column_summary}
\alias{build_column_summary}
\title{Build lightweight column summary for JS (names + types only)}
\usage{
build_column_summary(df)
}
\arguments{
\item{df}{A data frame}
}
\value{
A list of column summary objects
}
\description{
Returns \code{name}, \code{type}, \code{hasNA}, and \code{label} for each column — no unique
values. Sent on data arrival so the filter UI can render column dropdowns
instantly. Unique values are fetched on-demand via \code{\link[=build_column_values]{build_column_values()}}.
}
\details{
\code{label} is read from \code{attr(col, "label")} (e.g. ADaM datasets). It is
an empty string when the attribute is missing or not a length-1 character.

Exported as part of the public API so other blockr packages (e.g.
blockr.dm) can reuse the same column-metadata protocol.
}
