% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_js.R
\name{new_filter_js_block}
\alias{new_filter_js_block}
\title{JS-driven expression filter block (prototype)}
\usage{
new_filter_js_block(exprs = "TRUE", ...)
}
\arguments{
\item{exprs}{Reactive expression returning character vector of
filter conditions (default: "TRUE" for no filtering)}

\item{...}{Additional arguments forwarded to \code{\link[blockr.core:new_block]{blockr.core::new_block()}}}
}
\value{
A block object for expression-based filter operations
}
\description{
Like \code{\link[=new_filter_expr_block]{new_filter_expr_block()}} but all condition-row management (add, remove,
AND/OR toggle, expression editing) happens client-side in JavaScript.
R is only contacted when the user clicks \strong{Submit} — to parse and evaluate
the composed expression against the data.
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_board(
      blocks = list(
        a = new_dataset_block(),
        b = new_filter_js_block()
      ),
      links = links(from = "a", to = "b")
    )
  )
}
}
