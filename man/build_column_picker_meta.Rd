% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr-builders.R
\name{build_column_picker_meta}
\alias{build_column_picker_meta}
\title{Build a lightweight column summary for pickers (name + label)}
\usage{
build_column_picker_meta(df)
}
\arguments{
\item{df}{A data frame}
}
\value{
A list of \code{list(name, label)} entries, one per column.
}
\description{
Returns one entry per column: \code{list(name, label)}. Skips the \code{type}
detection and \code{anyNA()} sweep that \code{\link[=build_column_summary]{build_column_summary()}} does — those
are only needed by the filter block's type-aware condition UI. All other
picker blocks only need the name for the option value and the optional
label for the secondary text slot, so this helper is the lean path.
}
\details{
\code{label} is read from \code{attr(col, "label")} (e.g. ADaM datasets). It is
an empty string when the attribute is missing or not a length-1 character.
}
